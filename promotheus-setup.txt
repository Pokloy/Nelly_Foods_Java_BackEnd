Change this section on docker-compose.yml:

  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - C:/prometheus-3.5.0.windows-amd64/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
    networks:
      - ecommerce-network



Change the section "volumes:" which refers to the file location of prometheus.yml.
